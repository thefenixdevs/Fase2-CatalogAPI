apiVersion: v1
kind: Service
metadata:
  name: catalogapi-service
  namespace: catalogapi
  labels:
    app: catalogapi
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: catalogapi
